# Database
POSTGRES_DB=faultdb
POSTGRES_USER=faultuser
POSTGRES_PASSWORD=faultpass
DB_PORT=5433

# Backend
BACKEND_PORT=3001
NODE_ENV=production

# Frontend
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:3001
VITE_WS_URL=http://localhost:3001

# AI Dispatch Brief (optional — system works without it, uses template fallback)
# Gemini API key for AI-powered dispatch briefs
GEMINI_API_KEY=
# Legacy fallback: also accepts OPENAI_API_KEY
OPENAI_API_KEY=
